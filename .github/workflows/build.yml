name: Building and linting on pull request

on:
    pull_request:
        branches:
            - '**'

jobs:
    Continuous-Integration:

        name: Performing linting and building checks on pull request
        runs-on: ubuntu-latest

        steps:
        - name: Checkout code
          uses: actions/checkout@v3

        - name: Use Node.js
          uses: actions/setup-node@v3
          with:
           node-version: '20'  # Specify the Node.js version
       
        - name: Install dependencies
          run: npm install

        - name: Run linting checks
          uses: npm run lint

        - name: Run Build
          run: npm run build

    